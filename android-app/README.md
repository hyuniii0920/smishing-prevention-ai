# Android App (Kotlin + Firebase)

Open this folder in Android Studio.

Notes:
- Add your `google-services.json` to `android-app/app/`.
- Update plugin / Firebase versions as needed.
- Enable Firebase products in the console, then add dependencies in `app/build.gradle.kts`.
